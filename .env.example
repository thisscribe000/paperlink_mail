# Cloudflare
BOT_TOKEN=your_telegram_bot_token
BOT_SECRET=your_webapp_secret_key
APP_URL=https://paperlink.app

# D1 (auto-set by wrangler, here for local dev reference)
# DB=... (set via wrangler.toml binding)
# STORAGE=... (set via wrangler.toml binding)